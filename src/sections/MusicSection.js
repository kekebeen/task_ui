import React from 'react'

let MusicSection = React.createClass({
  render() {
    return (
      <section className='section' id="music">
        <h1>Music player section</h1>
      </section>
    );
  }
})

export default MusicSection
