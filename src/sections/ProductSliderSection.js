import React from 'react'

let ProductSliderSection = React.createClass({
  render() {
    return (
      <section className='section' id="slider">
        <h1>Product slider section</h1>
      </section>
    );
  }
})

export default ProductSliderSection
